一、原有架构存在的问题

原有架构中接收端分为两个独立进程——普通用户权限的receiver.exe负责接收消息，SYSTEM权限的cmdreceiver.exe负责执行系统命令。两个进程分别监听25105和25106两个端口，发送端需要根据操作类型选择不同端口发送，过于分散，缺乏统一管理。发送端只能手动输入目标IP，无法自动发现局域网内的设备。CMD命令采用落后的一问一答方式，执行完即退出，遇到执行后不自动退出的指令（如打开文件管理器）也难以获取明确的执行状态反馈，与真正的远程控制台体验相差甚远。

二、新版架构核心设计

新版将以receiver.exe这一Windows服务统一管理，加入系统服务实现开机自启。该服务在25105端口同时建立UDP和TCP监听，合并原有两个端口，根据消息内容中的类型和权限字段决定是自身执行还是转发给子进程。服务启动后以当前用户权限启动windowreceiver.exe和cmdreceiver.exe两个子进程，通过命名管道与之通信，并持续监控，子进程崩溃时自动重启。

三、组播自动发现

软件（发送端和接收端）首次启动时会生成唯一GUID保存到本地配置文件作为设备永久标识，用户卸载时可选择是否保留该配置文件。用户在发送端点击刷新按钮时，发送端向组播地址239.0.0.167发送查询包，在线接收端收到查询包后以单播回复本机的设备GUID、用户名及所有IP地址列表。发送端按设备GUID去重展示设备列表，同一设备GUID对应多个IP时全部记录，后续逐个尝试连接，以第一个成功的为准。用户名可随意修改，但设备GUID不变用于识别设备。接收端设置面板可灵活设置用户名和监听端口等。

四、普通消息沿用UDP协议

发送消息和结束消息将继续采用UDP发送至目标25105端口，响应沿用三行协议（OK/ERR/计数），轻量快速，且已经过验证确认可行，予以保留。

五、交互式CMD改用TCP协议

发送端与receiver.exe建立TCP持久连接。SYSTEM命令由receiver.exe直接执行，用户命令通过命名管道转交cmdreceiver.exe。cmdreceiver.exe维护长期CMD进程，重定向标准输入输出，实现目录切换、环境变量保持等交互式操作。TCP KeepAlive机制让发送端在连接断开时能够及时感知。

六、配置文件共享

同一台电脑的发送端和接收端操作同一个json配置文件，存储本机设备GUID和用户名等信息，按需读写，互不冲突。谁先运行谁创建，后运行的直接读取。任一端修改用户名后写入文件，另一端下次读取时会自动同步。

七、整体效果

两个进程+两个端口整合为一个Windows服务+一个25105端口+两个子进程。发送端从手动输入IP改为局域网内设备自动发现。UDP负责消息推送，TCP负责交互式CMD。receiver.exe统一调度，子进程负责消息展示和控制台，命名管道实现本地进程间通信。提示词系统由设备GUID+用户名+本地备注名机制替代，设备重命名后自动同步，无需重复配置。
